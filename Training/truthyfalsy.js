// what are the truthy and falsy values in JavaScript?
// Answer: Falsy values are values that are considered false when evaluated in a Boolean context. There are six falsy values in JavaScript. They are:
//false
// 0 and -0
// "" and '' (empty string)
// null
// undefined
// NaN (Not a Number)

//how to check empty array
 const myArr = [];
 if(myArr.length === 0){
    const push = 'usman'
    myArr.push(push)
    console.log(push)
 }

 //how to check empty object
 const myObj = {}
 if(Object.keys(myObj).length===0){
    console.log('empty object');
 }


